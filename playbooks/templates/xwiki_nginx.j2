server {
    server_name xwiki.{{ inventory_hostname }};

    listen 443 ssl; 
    ssl_certificate /etc/letsencrypt/live/xwiki.{{ inventory_hostname }}/fullchain.pem; 
    ssl_certificate_key /etc/letsencrypt/live/xwiki.{{ inventory_hostname }}/privkey.pem; 
    include /etc/letsencrypt/options-ssl-nginx.conf; 
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; 

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $http_host;
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;    
    	proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
	}
}
server {
    if ($host = xwiki.{{ inventory_hostname }}) {
        return 301 https://$host$request_uri;
    } 


    server_name xwiki.{{ inventory_hostname }};
    listen 80;
    return 404; 

}